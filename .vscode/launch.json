{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Active C++ File",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${fileDirname}",
            "preLaunchTask": "build active file"
        }
    ]
}
